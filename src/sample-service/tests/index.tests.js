/**
 * Application tests.
 * Follow a similar folder structures as src code when writing tests.
 * Ex: middleware tests are in tests/middleware folder.
 */
describe('main app test', () => {
   it('should run', () => {
        // require('../');
   });
});